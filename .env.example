# =============================================================================
# TTS-ALARMSERVER – Konfiguration
# Kopiere diese Datei nach .env und passe die Werte an.
# =============================================================================

# --- Server -------------------------------------------------------------------
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# --- Sicherheit ---------------------------------------------------------------
# Geheimer API-Schlüssel für geschützte Endpunkte (leer = kein Schutz)
API_KEY=

# --- Piper TTS ----------------------------------------------------------------
# Pfad zur Piper-Binary
PIPER_BINARY=/usr/local/bin/piper
# Verzeichnis mit den Voice-Modellen (*.onnx + *.onnx.json)
PIPER_VOICES_DIR=/app/voices
# Standard-Stimme (Dateiname ohne Erweiterung)
PIPER_DEFAULT_VOICE=de_DE-thorsten-high
# Spreachgeschwindigkeit (0.5 – 2.0, Standard: 1.0)
PIPER_SPEED=1.0
# Lautstärke (0 – 100)
PIPER_VOLUME=90

# --- Audio --------------------------------------------------------------------
# Verzeichnis für Gong-/Fanfare-Dateien
AUDIO_GONG_DIR=/app/gong
# Millisekunden Pause zwischen Gong und TTS
AUDIO_GONG_DELAY_MS=500
# Millisekunden Pause nach TTS
AUDIO_POST_DELAY_MS=1000

# --- RTP Streaming ------------------------------------------------------------
# Ziel-IP für RTP-Stream
RTP_HOST=239.255.0.1
# Ziel-Port für RTP-Stream
RTP_PORT=5004
# Bitrate des Audio-Streams
RTP_BITRATE=128k
# Codec (pcm_mulaw für G.711, libopus für Opus)
RTP_CODEC=libopus
# Abtastrate
RTP_SAMPLE_RATE=48000
# Multicast TTL
RTP_TTL=32
# Timeout in Sekunden bis Stream als hängend gilt
RTP_TIMEOUT_SEC=30

# --- Queue --------------------------------------------------------------------
# Maximale Warteschlangengröße
QUEUE_MAX_SIZE=50
# Prioritätsstufen: 1 (niedrig) – 10 (kritisch)
QUEUE_DEFAULT_PRIORITY=5

# --- Logging ------------------------------------------------------------------
# Log-Level: error, warn, info, http, debug
LOG_LEVEL=info
# Verzeichnis für Log-Dateien
LOG_DIR=/app/logs
# Maximale Log-Dateigröße
LOG_MAX_SIZE=20m
# Maximale Anzahl rotierter Log-Dateien
LOG_MAX_FILES=14

# --- Dashboard / WebSocket ----------------------------------------------------
# WebSocket Ping-Interval in Millisekunden
WS_PING_INTERVAL_MS=30000
# Maximale Einträge in der Alarmhistorie
HISTORY_MAX_ENTRIES=100

# --- Divera 24/7 Integration --------------------------------------------------
# Basis-URL der Divera-API
DIVERA_BASE_URL=https://www.divera247.com/api/v2
# Divera Access-Token (optional)
DIVERA_ACCESS_TOKEN=

# --- Prometheus (optional) ----------------------------------------------------
METRICS_ENABLED=false
METRICS_PATH=/metrics
